## 添加学号/工号字段完整计划

### 问题分析

当前系统用户表 `sys_user` 缺少学号/工号字段，导致个人信息配置页的学号/工号无法正常保存。

### 修改步骤

#### 1. 数据库表结构修改

**文件**: `docker/mysql/init/init.sql` 和 `backend/db_init.sql`

* 在 `sys_user` 表中添加 `student_id` 字段（VARCHAR(50)，唯一索引）

#### 2. 后端实体类修改

**文件**: `backend/src/main/java/com/library/seat/modules/sys/entity/SysUser.java`

* 添加 `studentId` 字段及对应的 getter/setter 方法

#### 3. 后端接口修改

**文件**: `backend/src/main/java/com/library/seat/modules/sys/controller/AuthController.java`

* 修改 `updateProfile` 接口，支持更新 `studentId` 字段

* 添加学号唯一性校验逻辑

#### 4. 前端页面修改

**文件**: `frontend/src/views/Profile/Info.vue`

* 修改表单提交逻辑，将 `studentId` 包含在提交数据中

* 确保学号/工号字段正确回填

#### 5. 前端 API 修改

**文件**: `frontend/src/api/auth.ts`（如需要）

* 确保 `updateProfile` API 传递 `studentId` 参数

### 预期结果

* 新用户注册后，学号/工号字段为空，需要用户自行填写

* 用户保存个人信息时，学号/工号能正确保存到数据库

* 学号/工号具有唯一性约束，防止重复

请确认后，我将开始执行修改。
